<div class="container">
  <h1>Playlists</h1>
    <%@list.each do |l|  %>
    <div class="card-product">
    <%= image_tag("") %>
      <div class="card-product-infos">
        <h2><%= link_to l.name, list_show_path(l.id) %></h2>
      </div>
    </div>
    <% end %>
</div>
